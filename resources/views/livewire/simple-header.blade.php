<div class="bg-gray-900 px-6 py-12 sm:py-12 lg:px-8">
      <div class="mx-auto max-w-2xl text-center">
        <h2 class="text-3xl font-bold tracking-tight text-white sm:text-5xl">{{$heading}}</h2>
        @if($subheading && $subheading != '')
        <p class="mt-6 text-lg leading-8 text-gray-300">{{$subheading}}</p>
        @endif
      </div>
</div>
